{
  "mcpServers": {
    "song2html": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/bin/song2html-mcp.js"],
      "env": {
        "SONG2HTML_PLUGIN_ROOT": "${CLAUDE_PLUGIN_ROOT}"
      }
    }
  }
}
